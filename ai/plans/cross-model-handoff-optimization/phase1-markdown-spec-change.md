# Phase 1 Markdown Spec Change

- Source of truth: `AI Coding Workflow 跨模型信息传递优化规划.md`, §6.1–6.2 and §16 Phase 1.
- Incorporated by: Codex, following the user's explicit request to continue from the Markdown plan.
- Supersedes for this slice: the earlier frozen contract's provisional S2 names
  `state-ir-v1.schema.json` and `generate_state_ir.py`.
- Implemented scope: the six Phase 1 deliverables named by the Markdown plan,
  plus one shared stdlib module, installer registration, documentation, and tests.
- Deferred: Handoff Delta/ACK protocol, evidence object storage, pull context,
  acceptance graph, ownership lease, and communication-aware routing.

The existing frozen contract remains the long-horizon architecture record. This
note prevents its provisional S2 filenames from silently overriding the newer,
explicit Phase 1 specification.
