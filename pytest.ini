[pytest]
testpaths =
    tests
norecursedirs =
    .git
    .worktrees
    .codegraph
    ai
    ref
    tmp-*
markers =
    integration: tests that create temporary repositories, worktrees, or invoke workflow scripts
    slow: higher-cost integration tests; skip in tight edit loops with -m "not slow"
