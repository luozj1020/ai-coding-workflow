# Stable workflow benchmark corpus

Fixture directories represent docs edits, Python helper fixes, shell portability,
installer idempotency, failed-test repair, C++/Bazel changes, large-repo location,
and remote handoff generation. Cases are metadata-only until populated with a
reviewed fixture; the runner never mutates repositories or invokes models.
